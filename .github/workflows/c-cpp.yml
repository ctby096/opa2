name: C/C++ CI

on:
  push:
    branches: [ "oneplus_sm8550_u_14.0.0_oneplus_ace2pro" ]
  pull_request:
    branches: [ "oneplus_sm8550_u_14.0.0_oneplus_ace2pro" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: configure
      run: ./configure
    - name: make
      run: make
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
